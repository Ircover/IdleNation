<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="workPlace"
            type="ircover.idlenation.adapters.WorkPlaceModel" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:textSize="@dimen/TextSizeMain"
            android:layout_margin="@dimen/MarginCommon"
            android:text="@{workPlace.name}"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:textSize="@dimen/TextSizeSecondary"
            android:layout_margin="@dimen/MarginCommon"
            android:text="@{workPlace.countString}"/>

    </LinearLayout>
</layout>